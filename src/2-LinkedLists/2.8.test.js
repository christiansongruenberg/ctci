import { expect } from 'chai'
import { LinkedList } from '../DataStructures/linkedList'
import { hasLoop } from './2.8'

describe('hasLoop() detects if there is a loop in the ll', function(){
    it('returns the node that ')
})